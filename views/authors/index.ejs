<h2>Search Authors</h2>
<form action="/authors" method="GET">
  <label>Name</label>
  <input type="text" name="name" value="<%= searchOptions.name %>" />
  <button type="submit">Search</button>
</form>
<br />
<!-- will create the list for each author where we show the names and buttons to view/edit/delete -->
<% authors.forEach(author => { %>
<div><%= author.name %></div>
<a href="/authors/<%= author.id %>">View</a>
<a href="/authors/<%= author.id %>/edit">Edit</a>
<!-- doing it this way means that though it is a POST the _method will get used instead and this will end up calling our router delete. This is because we set up _method inside of the server.js file as a method override with the method-override library -->
<%- include('../partials/deleteForm', { url: `/authors/${author.id}` }) %> <%})
%>
